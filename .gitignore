# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# SLURM logs
logs/

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.xdv
*.bbl
*.blg

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Unrelated to the Knesset Protocol Journal project (see CLAUDE.md project isolation rule)
al_nlp_benchmark/
exercises/

# Intermediate backup snapshots (segmentation.json.bak etc.)
*.bak

# Teammate (Tomer) separate pipeline — his own work/repo, not ours to commit
NLP_ADV/

# Step 12 large regenerable intermediates (segment text dumps)
steps/12_joint_pipeline/outputs/canon_input.json
steps/12_joint_pipeline/outputs/segments_*.json
steps/12_joint_pipeline/outputs/canonical_gold.json

# Canonicalization batch: the *inputs* are 5 MB of raw segment text, regenerable by
# split_batch.py from canon_input.json. The *outputs* (canonical_part_*.json) are the
# actual LLM work product and ARE committed.
handoff_canon_batch/segments_to_canonicalize.json
handoff_canon_batch/part_A.json
handoff_canon_batch/part_B.json

# Redundant with the extracted template/ directory
template.zip
