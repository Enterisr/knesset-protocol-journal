# Reports

## ✅ `acl/main.pdf` — **THIS IS THE FILE TO SUBMIT**

6 pages on the course ACL template (`template/`). Build:

```bash
cd report/acl
pdflatex main && bibtex main && pdflatex main && pdflatex main
```

Compiles with **0 overfull boxes, 0 undefined references/citations**. Sources:
`main.tex`, `bib.bib` (9 citations), `acl2023.sty`, `acl_natbib.bst`, `fig_gap.png`.

Structure follows the template exactly: Introduction / Data / Methods / Results, plus
Conclusion, Limitations and Reproducibility; all 6 tables and the figure sit on the
dedicated float page after `\clearpage`.

`\usepackage{ACL2023}` — the `[review]` option (margin line numbers) is deliberately
**off**, since this is the final version.

## ❌ `knesset_journal_report.pdf` — superseded, do not submit

The earlier 3-page version. It is a hand-rolled `twocolumn` layout that does **not** use
the course template, has **no citations**, uses narrative rather than the prescribed
section names, and sets its tables inline. Kept only for reference.

## Numbers

Every figure in `acl/main.pdf` was validated against `steps/*/outputs/*.json` on
2026-08-01. Three did not survive and were fixed at the source rather than in the prose:

| claim | was | now | fixed in |
|---|---|---|---|
| fingerprint fusion F1 | 0.211 | **0.2407** (and batch-fit, so not comparable to 0.190) | `steps/10/code/make_plots.py`, Step 10 README |
| end-to-end journal at θ=0.342 | 493 / 23 / 0.954 | **491 / 25 / 0.950** | `steps/12/code/verify_operating_points.py` |
| lever table | mixed batch and honest rows | **all rows matched** (0.086 / 0.087 / 0.190) | `steps/10/code/honest_levers.py` |

`fig_gap.png` is regenerated by `make_report_figs.py`. No figure or table in the report
contains a hard-coded value.
